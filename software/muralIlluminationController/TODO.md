Don't write to the pwm controller if the frequency remains constant
Create generic illumination controlller?
Create moon (illumination + static)

set configuration globals?
set configuration interface

test pwm channels